name: targeting-service
on:
  pull_request: { paths: ['services/targeting-service/**', '.github/workflows/**'] }
  push: { branches: [main], paths: ['services/targeting-service/**', '.github/workflows/**'] }
jobs:
  devsecops:
    uses: ./.github/workflows/reusable-devsecops.yml
    with: { service: targeting-service, language: python }
    secrets: inherit

